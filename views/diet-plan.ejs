<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <title>Günlük Programım - Lifex</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body class="bg-diet-page">

    <%- include('partials/navbar') %>

        <div class="container py-5">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2 class="fw-bold">Bugünkü Beslenme Planın</h2>
                <a href="/dashboard/<%= user._id %>" class="btn btn-outline-secondary">Panela Dön</a>
            </div>

            <!-- Özet Kartı -->
            <div class="alert alert-info shadow-sm">
                <h4 class="alert-heading">Hedef vs Planlanan</h4>
                <p class="mb-0">
                    Hedefin: <strong>
                        <%= user.dailyCalories.toFixed(1) %> kcal
                    </strong> |
                    Bu Listenin Toplamı: <strong>
                        <%= totals.totalCalories.toFixed(1) %> kcal
                    </strong>
                </p>
                <hr>
                <div class="d-flex justify-content-around text-center">
                    <div>Protein: <b>
                            <%= totals.totalProtein.toFixed(1) %>g
                        </b></div>
                    <div>Karbonhidrat: <b>
                            <%= totals.totalCarbs.toFixed(1) %>g
                        </b></div>
                    <div>Yağ: <b>
                            <%= totals.totalFat.toFixed(1) %>g
                        </b></div>
                </div>
            </div>

            <div class="row g-4">
                <!-- Kahvaltı -->
                <div class="col-md-12">
                    <div class="card border-warning border-2 shadow-sm">
                        <div class="card-header bg-warning text-dark fw-bold">Kahvaltı</div>
                        <div class="card-body d-flex justify-content-between align-items-center">
                            <div>
                                <h5 class="card-title mb-1">
                                    <%= plan.breakfast.name %>
                                </h5>
                                <small class="text-muted">Güzel bir başlangıç</small>
                            </div>
                            <span class="badge bg-dark rounded-pill">
                                <%= plan.breakfast.calories.toFixed(1) %> kcal
                            </span>
                        </div>
                    </div>
                </div>

                <!-- Öğle -->
                <div class="col-md-6">
                    <div class="card border-primary shadow-sm h-100">
                        <div class="card-header bg-primary text-white fw-bold">Öğle Yemeği</div>
                        <div class="card-body">
                            <h5 class="card-title">
                                <%= plan.lunch.name %>
                            </h5>
                            <ul class="list-unstyled small text-muted mt-3">
                                <li>Protein: <%= plan.lunch.protein %>g</li>
                                <li>Karb: <%= plan.lunch.carbs %>g</li>
                                <li>Yağ: <%= plan.lunch.fat %>g</li>
                            </ul>
                            <div class="text-end fw-bold text-primary">
                                <%= plan.lunch.calories %> kcal
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Akşam -->
                <div class="col-md-6">
                    <div class="card border-primary shadow-sm h-100">
                        <div class="card-header bg-primary text-white fw-bold">Akşam Yemeği</div>
                        <div class="card-body">
                            <h5 class="card-title">
                                <%= plan.dinner.name %>
                            </h5>
                            <ul class="list-unstyled small text-muted mt-3">
                                <li>Protein: <%= plan.dinner.protein %>g</li>
                                <li>Karb: <%= plan.dinner.carbs %>g</li>
                                <li>Yağ: <%= plan.dinner.fat %>g</li>
                            </ul>
                            <div class="text-end fw-bold text-primary">
                                <%= plan.dinner.calories %> kcal
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Ara Öğünler -->
                <div class="col-md-12">
                    <div class="card border-success shadow-sm">
                        <div class="card-header bg-success text-white fw-bold">Ara Öğünler (Atıştırmalık)</div>
                        <div class="card-body">
                            <ul class="list-group list-group-flush">
                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                    1. <%= plan.snack1.name %>
                                        <span class="badge bg-secondary rounded-pill">
                                            <%= plan.snack1.calories %> kcal
                                        </span>
                                </li>
                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                    2. <%= plan.snack2.name %>
                                        <span class="badge bg-secondary rounded-pill">
                                            <%= plan.snack2.calories %> kcal
                                        </span>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>

                <!-- Yenile Butonu -->
                <div class="col-12 text-center mt-3">
                    <a href="/generate-diet/<%= user._id %>" class="btn btn-outline-primary">🔄 Listeyi Beğenmedim,
                        Yenisini
                        Yap</a>
                </div>

            </div>
        </div>
        <%- include('partials/footer') %>
            <script src="/js/bootstrap.bundle.min.js"></script>
</body>

</html>
<!DOCTYPE html>
<html lang="tr">
<head>
    <title>Üyeliğim - LIFEX</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/all.min.css">
</head>
<body class="bg-membership-page" style="padding-top: 100px; background-image: url('/images/membership-bg.png');">
    
    <%- include('partials/navbar') %>

    <!-- HESAPLAMA MANTIĞI (Frontend tarafında) -->
    <% 
        const start = new Date(membership.startDate);
        const end = new Date(membership.endDate);
        const now = new Date();
        
        // Kalan Gün Hesabı
        const diffTime = Math.abs(end - now);
        const remainingDays = Math.ceil(diffTime / (1000 * 60 * 60 * 24));
        
        // Progress Bar Hesabı
        const totalDuration = end - start;
        const elapsed = now - start;
        let percentage = Math.round((elapsed / totalDuration) * 100);
        if(percentage > 100) percentage = 100;
        if(percentage < 0) percentage = 0;
    %>

    <div class="container pb-5">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                
                <h2 class="text-white fw-bold mb-4 text-center">Üyelik Durumum</h2>

                <!-- PREMIUM KART -->
                <div class="card membership-card gold-glow p-4 p-md-5">
                    
                    <!-- Kart Üstü -->
                    <div class="d-flex justify-content-between align-items-start mb-4">
                        <div class="chip"></div>
                        <div class="text-end">
                            <h3 class="text-warning fw-bold fst-italic mb-0">LIFEX MEMBER</h3>
                            <small class="text-white opacity-75">Premium Access</small>
                        </div>
                    </div>

                    <!-- Paket Adı -->
                    <div class="mb-5">
                        <span class="info-label">Mevcut Paket</span>
                        <h1 class="display-5 fw-bold text-white text-uppercase" style="letter-spacing: 2px; text-shadow: 0 2px 10px rgba(0,0,0,0.5);">
                            <%= membership.planName %>
                        </h1>
                    </div>

                    <!-- Detaylar Grid -->
                    <div class="row g-4 mb-4">
                        <div class="col-6 col-md-3">
                            <div class="info-label">Durum</div>
                            <div class="info-value">
                                <span class="badge bg-success bg-opacity-75 border border-success px-3">AKTİF</span>
                            </div>
                        </div>
                        <div class="col-6 col-md-3">
                            <div class="info-label">Başlangıç</div>
                            <div class="info-value"><%= start.toLocaleDateString('tr-TR') %></div>
                        </div>
                        <div class="col-6 col-md-3">
                            <div class="info-label">Bitiş</div>
                            <div class="info-value"><%= end.toLocaleDateString('tr-TR') %></div>
                        </div>
                        <div class="col-6 col-md-3">
                            <div class="info-label">Ücret</div>
                            <div class="info-value"><%= membership.pricePaid %> ₺</div>
                        </div>
                    </div>

                    <!-- İlerleme Çubuğu (Zaman) -->
                    <div class="mt-4">
                        <div class="d-flex justify-content-between text-light mb-2">
                            <small>Üyelik Süreci</small>
                            <small class="fw-bold text-warning"><%= remainingDays %> Gün Kaldı</small>
                        </div>
                        <div class="progress" style="height: 8px; background-color: rgba(255,255,255,0.1);">
                            <div class="progress-bar bg-warning" role="progressbar" style="width: <%= percentage %>%"></div>
                        </div>
                        <div class="d-flex justify-content-between mt-1" style="font-size: 10px; color: #aaa;">
                            <span>%0</span>
                            <span>%<%= percentage %> Tamamlandı</span>
                            <span>%100</span>
                        </div>
                    </div>

                </div>

                <!-- Aksiyon Butonları -->
                <div class="row mt-4 g-3">
                    <div class="col-md-6">
                        <a href="/clubs" class="btn btn-outline-light w-100 py-3 fw-bold">
                            <i class="fa-solid fa-location-dot me-2"></i> KULÜPLERİ KEŞFET
                        </a>
                    </div>
                    <div class="col-md-6">
                        <a href="/classes" class="btn btn-warning w-100 py-3 fw-bold text-dark">
                            <i class="fa-regular fa-calendar-check me-2"></i> DERS REZERVASYONU YAP
                        </a>
                    </div>
                </div>

                <!-- Yenileme Uyarısı (Eğer 7 günden az kaldıysa göster) -->
                <% if(remainingDays <= 7) { %>
                    <div class="alert alert-danger mt-4 d-flex align-items-center" role="alert">
                        <i class="fa-solid fa-triangle-exclamation me-3 fa-2x"></i>
                        <div>
                            <strong>Dikkat!</strong> Üyeliğinizin bitmesine <%= remainingDays %> gün kaldı. 
                            Kesintisiz spor için <a href="/membership" class="alert-link">paketinizi yenileyin.</a>
                        </div>
                    </div>
                <% } %>

            </div>
        </div>
    </div>

    <%- include('partials/footer') %>
    <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>